const KEYCHAIN_STATE_OFF = 0
const KEYCHAIN_STATE_ON = 1

const MAX_PASSWORD_LENGTH_BYTES = 64;
const EXTRA_PADDING_BYTES = 4;
const TOTAL_PASSWORD_LENGTH =
	MAX_PASSWORD_LENGTH_BYTES + 1 + EXTRA_PADDING_BYTES;
const AES_KEY_LENGTH_BITS = 2 * MAX_PASSWORD_LENGTH_BYTES;
const MAC_KEY_LENGTH_BITS = 4 * MAX_PASSWORD_LENGTH_BYTES;


module.exports = {
	KEYCHAIN_STATE_OFF,
	KEYCHAIN_STATE_ON,
	MAX_PASSWORD_LENGTH_BYTES,
	EXTRA_PADDING_BYTES,
	TOTAL_PASSWORD_LENGTH,
	AES_KEY_LENGTH_BITS,
	MAC_KEY_LENGTH_BITS
}