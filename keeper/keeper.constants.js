export const KEYCHAIN_STATE_OFF = 0
export const KEYCHAIN_STATE_ON = 1

export const MAX_PASSWORD_LENGTH_BYTES = 64;
export const EXTRA_PADDING_BYTES = 4;
export const TOTAL_PASSWORD_LENGTH =
	MAX_PASSWORD_LENGTH_BYTES + 1 + EXTRA_PADDING_BYTES;
export const AES_KEY_LENGTH_BITS = 2 * MAX_PASSWORD_LENGTH_BYTES;
export const MAC_KEY_LENGTH_BITS = 4 * MAX_PASSWORD_LENGTH_BYTES;
